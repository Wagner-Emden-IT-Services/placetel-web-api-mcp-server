# Dependencies / build output
node_modules/
dist/
*.tsbuildinfo

# Secrets — NEVER commit
.env
.env*.local
*.key

# Local tooling / internal build artifacts (not for publication)
.claude/
.mcp.json
BUILD-PROMPT-placetel-mcp.md

# Logs / OS / editor
*.log
.DS_Store
Thumbs.db
.history/
